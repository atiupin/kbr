[dosbox]
working directory option=config
captures=tmp

[cpu]
# the debugger needs the interpreting core -- breakpoints are unreliable on the others
core=normal

[render]
aspect=true

[autoexec]
mount c build
c:
