[dosbox]
captures=tmp
working directory option=custom
working directory default=tmp

[cpu]
# the debugger needs the interpreting core -- breakpoints are unreliable on the others
core=normal

[render]
aspect=true

[autoexec]
mount c build
c:
@echo King's Bounty (Russian), run KBR
