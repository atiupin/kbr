{
  "name": "kbr",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "typecheck": "tsc -p core && tsc -p cli",
    "kbr": "node cli/main.ts",
    "build": "node cli/main.ts build",
    "verify": "node cli/main.ts verify",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/pngjs": "^6.0.5",
    "prettier": "^3.9.6",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@noble/hashes": "^2.2.0",
    "pngjs": "^7.0.0"
  }
}
